# Story bank

Synthetic fixture data. This fixture exists to pin the rejection path, so the
bank stays minimal.

## Gamma — Backend Developer

Ran the payments webhook pipeline on Python and MySQL. Read a lot about Kafka
but never shipped anything on it.
