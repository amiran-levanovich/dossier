# Alias swap log

The assembled CV was proven verbatim against `master_cv.md` **before** these swaps were applied (ADR-0008), so this list is the complete set of differences between the delivered `cv.md` and the exemplar's text.

Tables read: `scripts/alias_groups.md`

## Swaps (1)

- line 22: `Rails` → `Ruby on Rails`
